---
title: 'Get Current User'
api: 'GET /api/auth/me'
description: 'Retrieve the currently authenticated user information'
---

## Endpoint

Returns the user profile and account information for the currently authenticated session.

## Authentication

Requires a valid session token via:
- `Authorization: Bearer <token>` header, or
- `heimdall_session` cookie

## Request

No request body or query parameters required.

### Example

<CodeGroup>

```bash cURL
curl https://heimdall.example.com/api/auth/me \
  -H "Authorization: Bearer <your_session_token>"
```

```javascript JavaScript
const response = await fetch('https://heimdall.example.com/api/auth/me', {
  headers: {
    'Authorization': 'Bearer <your_session_token>'
  }
});

const user = await response.json();
console.log(user);
```

```python Python
import requests

response = requests.get(
    'https://heimdall.example.com/api/auth/me',
    headers={'Authorization': 'Bearer <your_session_token>'}
)

print(response.json())
```

</CodeGroup>

## Response

### Success Response

<ResponseField name="success" type="boolean">
  Always `true` for successful requests
</ResponseField>

<ResponseField name="data" type="object">
  User object
  
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      User ID (UUID v7)
    </ResponseField>
    
    <ResponseField name="email" type="string">
      User email address
    </ResponseField>
    
    <ResponseField name="display_name" type="string | null">
      Display name (null if not set)
    </ResponseField>
    
    <ResponseField name="role" type="string">
      User role (typically "user")
    </ResponseField>
    
    <ResponseField name="created_at" type="string">
      Account creation timestamp (ISO 8601)
    </ResponseField>
    
    <ResponseField name="updated_at" type="string">
      Last update timestamp (ISO 8601)
    </ResponseField>
    
    <ResponseField name="github_username" type="string | null">
      Linked GitHub username (if connected)
    </ResponseField>
    
    <ResponseField name="gitlab_username" type="string | null">
      Linked GitLab username (if connected)
    </ResponseField>
  </Expandable>
</ResponseField>

```json
{
  "success": true,
  "data": {
    "id": "01932e4a-7b2c-7890-abcd-1234567890ab",
    "email": "alice@example.com",
    "display_name": "Alice",
    "role": "user",
    "created_at": "2026-03-10T10:30:00Z",
    "updated_at": "2026-03-12T15:45:00Z",
    "github_username": "alice",
    "gitlab_username": null
  }
}
```

### Error Responses

<AccordionGroup>
  <Accordion title="401 Unauthorized - No token">
    ```json
    {
      "success": false,
      "error": {
        "code": 401,
        "message": "Authentication required"
      }
    }
    ```
  </Accordion>

  <Accordion title="401 Unauthorized - Invalid token">
    ```json
    {
      "success": false,
      "error": {
        "code": 401,
        "message": "Invalid or expired session"
      }
    }
    ```
  </Accordion>

  <Accordion title="500 Internal Server Error">
    ```json
    {
      "success": false,
      "error": {
        "code": 500,
        "message": "Internal server error"
      }
    }
    ```
  </Accordion>
</AccordionGroup>

## Use Cases

- **Profile display** — Show user information in navigation bar or settings
- **Permission checks** — Verify user role before showing admin features
- **Session validation** — Check if the current session is still valid
- **Account linking status** — Determine which OAuth providers are connected

## Related Endpoints

- [Login](/api/authentication/login) - Authenticate and create a session
- [Logout](/api/authentication/logout) - End the current session
- [Update Profile](/api/settings/profile) - Modify user display name
